---
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import GoogleTagManagerBody from "../components/analytics/GoogleTagManagerBody.astro";
import GoogleTagManagerHead from "../components/analytics/GoogleTagManagerHead.astro";
import MetaPixel from "../components/analytics/MetaPixel.astro";
import MetaPixelEvents from "../components/analytics/MetaPixelEvents.astro";

interface Props {
  title?: string;
  description?: string;
  chrome?: "default" | "landing";
  ctaHref?: string;
  ctaLabel?: string;
}

const {
  title = "MaaxGen",
  description = "Marketing, advertising, and AI for local and e-commerce businesses.",
  chrome = "default",
  ctaHref = "/contact",
  ctaLabel = "Get started",
} = Astro.props;

const landing = chrome === "landing";
const pageTitle = title === "MaaxGen" ? title : `${title} | MaaxGen`;
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <GoogleTagManagerHead />
    <link rel="icon" type="image/svg+xml" href="/brand/Maaxgen Vector Logo Icon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <meta name="generator" content={Astro.generator} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;700;800&display=swap"
      rel="stylesheet"
    />
    <title>{pageTitle}</title>
    <MetaPixel />
    <script is:inline>document.documentElement.classList.add("js");</script>
  </head>
  <body>
    <GoogleTagManagerBody />
    <Header
      landing={landing}
      ctaHref={landing ? ctaHref : "/contact"}
      ctaLabel={landing ? ctaLabel : "Get started"}
    />
    <main>
      <slot />
    </main>
    <Footer landing={landing} />
    <MetaPixelEvents />
    <script>
      const targets = document.querySelectorAll("[data-reveal]");
      const reduced = window.matchMedia("(prefers-reduced-motion: reduce)").matches;

      if (reduced || !("IntersectionObserver" in window)) {
        targets.forEach((el) => el.classList.add("is-revealed"));
      } else {
        const observer = new IntersectionObserver(
          (entries) => {
            for (const entry of entries) {
              if (entry.isIntersecting) {
                entry.target.classList.add("is-revealed");
                observer.unobserve(entry.target);
              }
            }
          },
          { rootMargin: "0px 0px -8% 0px", threshold: 0.15 }
        );
        targets.forEach((el) => observer.observe(el));
      }
    </script>
  </body>
</html>

<style is:global>
  @import "../styles/global.css";
</style>
